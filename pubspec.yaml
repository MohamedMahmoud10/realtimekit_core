name: realtimekit_core_platform_interface
description: A common platform interface for the realtimekit_core plugin.
version: 0.1.5
homepage: https://docs.realtime.cloudflare.com/flutter-core
documentation: https://docs.realtime.cloudflare.com/flutter-core

environment:
  sdk: ">=2.19.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  focus_detector_v2: ^3.1.0+1
  plugin_platform_interface: ^2.1.8

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
