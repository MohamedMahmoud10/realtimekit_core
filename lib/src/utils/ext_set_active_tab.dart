import 'package:realtimekit_core_platform_interface/realtimekit_core_platform_interface.dart';

extension RtkMetaExtension on RtkMeta {
  set setActiveTab(ActiveTab? activeTab) {
    this.activeTab = activeTab;
  }
}
